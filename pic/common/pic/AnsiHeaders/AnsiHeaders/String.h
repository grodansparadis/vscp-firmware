#ifndef STRING_H
#define STRING_H

#include <Strings.h>

/*
 * We include mem.h because memset() is there instead of here in string.h where it usually is
 */ 
#include <mem.h>

#endif	/* STRING_H */