/* Stub ESP-IDF esp_log_buffer.h for desktop unit-test builds. */
#pragma once
#define ESP_LOG_BUFFER_HEXDUMP(tag, buf, len, level) ((void)(tag))
#define ESP_LOG_BUFFER_HEX(tag, buf, len)            ((void)(tag))
